export default class Class {
    constructor (
        private id: string,
        private name: string,
        private module: string
    ) {
        this.id = id
        this.name = name
        this.module = module
    }
}